/** host/constants.ts — 宿主侧私有常量（跨 half 协议常量见 shared/constants.ts）。 */

/** 插件自持状态目录（旧版归档迁移用，位于 $DSH_HOME 下）。 */
export const SESSION_STATE_DIRECTORY = 'dsh-tauri-session'
/** 旧版（v1）自持归档文件名。 */
export const SESSION_ARCHIVE_FILE = 'archive.json'
